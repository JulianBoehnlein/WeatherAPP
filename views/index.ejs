<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.14.0/css/all.css">
    <link rel="stylesheet" href="http://localhost:3000/styles/styles.css">
    
</head>
<body>
   <form action="/weather" method="POST" class="input"> 
    <input type="text" name="city" placeholder="Enter city name">
    <input type="submit" value="Get Weather">
   </form>
   <%if (locals.error) { %> 
<div class="error-box">
    <img src="./images/error.png" alt="">  
    <p class="errortxt"> <%=error%> </p>
</div>

      
<% } else if (locals.data) { %>

<div class="box">
   <div class="content">
      <ul>
           <li>
               <h1 class="city"><%=locals.city%></h1>
           </li>
           <li>
               <img src= <%=locals.img%> alt="">
           </li>
           <li>
               <p class="temp"><%=Math.round(data.main.temp)%>&#176C <i class="fas fa-thermometer-half"></i></p>
           </li>
           <li>
               <p class="condition"><%=data.weather[0].description%></p>
           </li>
           <li>
               <p class="high">H:<%=Math.round(data.main.temp_max)%>&#176 T:<%=Math.round(data.main.temp_max)%>&#176</p>
               <!-- Hätte ich feature mit mehreren Tagen, hätte ich mit index[0] arbeiten müssen, um ersten Tag zu bekommen. Leider kostenpflichtig -->
            </li>   
       </ul>
       <img class="rain" src="mages/raingif.gif" alt="">
    </div>
</div>
<% } %>

<script src="/client.js"></script>
</body>
</html>